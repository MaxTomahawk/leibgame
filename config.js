// Supabase credentials — copy from config.example.js after creating a project.
// When empty, the game runs in offline mode (localStorage only).

window.env = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: ''
};
